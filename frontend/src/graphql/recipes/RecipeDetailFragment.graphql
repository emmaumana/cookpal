fragment RecipeDetail on Recipe {
  id
  title
  slug
  totalTime
  imageUrl
  rating
  createdAt
  categories {
    nodes {
      id
      name
    }
  }
  ingredients {
    nodes {
      id
      title
    }
  }
}
